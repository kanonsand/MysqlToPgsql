server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://10.134.4.108:54321/testdb
    username: gpadmin
    password: greenplum@dptech
    driver-class-name: org.postgresql.Driver

mybatis:
  mapper-locations: classpath:mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true

# Conversion settings
conversion:
  enabled: true
  strict-mode: false

# Logging configuration
logging:
  level:
    root: INFO
    converter.mybatis: DEBUG
    org.example.server: DEBUG
    org.mybatis: DEBUG
